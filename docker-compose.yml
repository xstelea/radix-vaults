services:
  postgres:
    image: postgres:17
    container_name: radix-vaults-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: radix_vaults
    ports:
      - '5433:5432'
    volumes:
      - radix-vaults-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d radix_vaults']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  radix-vaults-postgres:

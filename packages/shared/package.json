{
  "name": "@radix-vaults/shared",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsgo -p tsconfig.json --emitDeclarationOnly false --noEmit false --outDir dist",
    "dev": "tsx --watch src/index.ts",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests",
    "check-types": "tsgo --noEmit"
  },
  "dependencies": {
    "@effect/platform": "catalog:",
    "@radix-effects/shared": "catalog:",
    "effect": "^3.19.19"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "oxlint": "catalog:",
    "tsx": "catalog:",
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
